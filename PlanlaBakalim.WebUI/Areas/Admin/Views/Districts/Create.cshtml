@model PlanlaBakalim.Core.Entities.District
@{
    ViewData["Title"] = "Yeni İlçe";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2><i class="fas fa-building"></i> Yeni İlçe</h2>
    <a href="@Url.Action("Index")" class="btn btn-secondary">
        <i class="fas fa-arrow-left"></i> Geri Dön
    </a>
</div>

<div class="card">
    <div class="card-header">
        <h5 class="card-title mb-0">
            <i class="fas fa-plus"></i> İlçe Bilgileri
        </h5>
    </div>
    <div class="card-body">
        <form asp-action="Create" method="post">
            <div class="row">
                <div class="col-md-6">
                    <div class="mb-3">
                        <label asp-for="CityId" class="form-label">İl</label>
                        <select asp-for="CityId" class="form-select" required>
                            <option value="">İl Seçin</option>
                            @foreach (var city in ViewBag.Cities as List<PlanlaBakalim.Core.Entities.City>)
                            {
                                <option value="@city.Id">@city.Name</option>
                            }
                        </select>
                        <span asp-validation-for="CityId" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="mb-3">
                        <label asp-for="Name" class="form-label">İlçe Adı</label>
                        <input asp-for="Name" class="form-control" placeholder="İlçe adını girin" required />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                </div>
            </div>
            
            <div class="d-flex justify-content-end">
                <a href="@Url.Action("Index")" class="btn btn-secondary me-2">
                    <i class="fas fa-times"></i> İptal
                </a>
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-save"></i> Kaydet
                </button>
            </div>
        </form>
    </div>
</div>

<div class="row mt-4">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">
                    <i class="fas fa-info-circle"></i> Bilgi
                </h5>
            </div>
            <div class="card-body">
                <div class="alert alert-info">
                    <h6><i class="fas fa-lightbulb"></i> Önemli Notlar:</h6>
                    <ul class="mb-0">
                        <li>İlçe adı seçilen il içinde benzersiz olmalıdır</li>
                        <li>İlçe oluşturulduktan sonra işletme adresleri ekleyebilirsiniz</li>
                        <li>İlçe silmek için önce tüm işletme adreslerini silmeniz gerekir</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
